{
  "name": "design-house",
  "version": "0.1.0",
  "description": "Local room-by-room redesign studio. Generate AI redesigns of each room from uploaded photos, then export a build report (paint, wood, decor).",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1"
  }
}
